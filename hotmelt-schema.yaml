rules:
  configs:
    ErrorThreshold:
      type: string
      required: true
      editableAt:
        - line
      editableBy:
        - OT
    ${{$include(LogsettingsSchema/1.0.1)}}:
    ${{$include(ProtocolSchema/1.0.1, TCP)}}:
    TempSettings:
      type: object
      required: true
    TempSettings.Threshold:
      type: int
      required: true
      editableAt:
        - line
      editableBy:
        - OT

metadata:
  name: HotmeltSchema3
  version: 1.0.2